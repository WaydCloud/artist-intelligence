# DESIGN — 클라이언트 대면 하네스 (브랜드·UI 규격)

> **클라이언트에 접근하는 모든 코드(페이지·컴포넌트·문구)는 이 문서를 거친다.** 위반은 리뷰에서 차단([`AGENTS.md`](AGENTS.md) §7).
> 아티스트 "브랜드" 분석([`docs/02-trend-brand.md`](docs/02-trend-brand.md)·[`docs/03-brand-harness.md`](docs/03-brand-harness.md))과는 무관한 문서다 — 여기서 브랜드는 **우리 서비스의 브랜드**를 뜻한다.

## 0. 정체성

- **브랜드명(대외 표기): `WaydCloud`** — 워드마크는 대문자 `WAYDCLOUD`, 디스플레이 서체 + 넓은 자간.
- 제품: Artist Intelligence (리포트 대시보드), Utilities (보조 도구).
- 미학 키워드 6: **시네마틱 · 미니멀리즘 · FUTURA · PRETENDARD · 에디토리얼 · 글래스모피즘 · 메탈릭**. 아래 §1이 키워드를 집행 가능한 규칙으로 번역한다.

## 1. 미학 원칙 (키워드 → 규칙)

| 키워드 | 규칙 |
|---|---|
| 시네마틱 | 넓은 여백 + 큰 명암 대비 + 배경은 은은한 라디얼 글로우/비네트/필름 그레인(토큰 `--glow`·`--vignette`·`--grain`)까지만. 그레인은 저불투명 오버레이(`--grain-opacity`, ≤ 0.08) — 텍스트 대비를 깎으면 뺀다. 사진·영상 배경 금지(정적 우선). |
| 미니멀리즘 | 표면당 시각적 강조 1개. 장식은 기능(위계·상태)에 종속. 요소를 더하기 전에 뺄 것을 먼저 찾는다. |
| 에디토리얼 | 구조는 타이포 위계로 전달(크기·굵기·자간). 본문 줄길이 ≤ 68ch. 그리드 정렬 이탈 금지. |
| 글래스모피즘 | **표면 위계 표현 전용**(카드·네비). 토큰 `--glass-bg`·`--glass-border` + blur만 사용. 차트·표·본문 텍스트 뒤에는 금지(§4). |
| 메탈릭 | **포인트 액센트 전용**(워드마크·제목·아이콘·경계 하이라이트). 토큰 `--metal` 그라디언트만. 대면적 배경 금지. |

## 2. 타이포그래피

- **디스플레이(워드마크·대제목)**: Futura → 폴백 **Jost**(무료 기하학적 산세리프, next/font 셀프호스트). Futura는 상용 라이선스 — 웹폰트 라이선스를 확보하기 전까지 웹에서는 Jost가 실제 렌더 서체다(스택에 Futura를 앞세워 로컬 설치 환경만 Futura로 렌더).
- **본문·UI·한글**: **Pretendard**(Variable, CDN).
- 스택 정본은 `apps/dashboard/app/globals.css`의 `--font-display`·`--font-body`. 컴포넌트에서 폰트명 직접 지정 금지.

## 3. 토큰 (단일 정본)

- **정본: [`apps/dashboard/app/globals.css`](apps/dashboard/app/globals.css) CSS 변수.** 컴포넌트에는 `var(--…)` 참조만 — hex·rgb 하드코딩 금지.
- **dataviz 토큰**(`--surface`·`--ink`·`--series`… )은 검증된 팔레트다. 현행: **팔레트 v2**(2026-07-20, 시네마틱 바이올렛 주조 — 라이트 `#6d28d9/#b45309/#0891b2`, 다크 `#8b5cf6/#d97706/#0891b2`). 변경 절차: dataviz 스킬 검증기 6종(명도 밴드·채도 플로어·CVD 분리·정상시 플로어·표면 대비) **라이트/다크 모두 통과** + 히트맵 시퀀셜 램프는 단조 명도 유지. 통과 기록 없이 값 변경 금지.
- **브랜드 토큰**(추가분): `--font-display` `--font-body` `--glass-bg` `--glass-border` `--metal` `--glow` `--vignette` `--grain` `--grain-opacity`. 라이트/다크 각각 정의.
- 브랜드 표면의 배경 레이어(글로우·비네트·그레인)는 `components/BrandBackdrop.tsx` 하나로만 렌더 — 페이지마다 재구현 금지.

## 4. 표면 위계 (어디에 무엇을 쓰나)

**전 표면에 브랜드 미학(시네마틱·글래스·메탈릭·그레인)을 적용한다.** 위계는 "장식 가능 여부"가 아니라 **증거와의 거리**로 긋는다:

| 레이어 | 범위 | 규칙 |
|---|---|---|
| **배경** | 페이지 배경(랜딩·대시보드 공통) | 글로우·그레인 허용. 렌더는 `BrandBackdrop` 하나로만 |
| **크롬** | 헤더/네비, 카드 프레임, 섹션 제목, 탭 | 글래스(`.glass-card` 단일 정의) + 메탈릭 액센트 + 디스플레이 서체 |
| **증거** | 차트 플롯 영역, 표 셀, 지표 숫자, 본문 텍스트 | **dataviz 토큰만.** 텍스트·데이터 마크 *위나 바로 뒤*에 오버레이·그라디언트 금지 — 증거 가독성이 브랜드보다 우선(책임소재 불변식의 연장) |

## 5. 모션

- 마이크로 인터랙션 150–300ms `ease-out`, 대형 전환 ≤ 400ms. 무한 반복 애니메이션 금지.
- `prefers-reduced-motion` 존중(전환 제거 또는 즉시 완료).

## 6. 접근성·문구 불변식

- 라이트/다크 **둘 다** 성립해야 머지 가능. 본문 대비 WCAG AA(4.5:1) 이상.
- 글래스 표면 위 텍스트가 대비를 확보하지 못하면 글래스를 포기한다(반대가 아니라).
- UI 카피도 [`AGENTS.md`](AGENTS.md) §5를 따른다: 단정·과대주장 금지, 신호·참고 어법.

### 6.1 카피 스타일 (클라이언트 대면 문구 한정 — 내부 문서 제외)

> 적용 대상엔 **모듈이 생성하는 표시 문구**(report.json의 `title`·`insights`·`recommendations`·`note`)도 포함된다 — 대시보드에 그대로 렌더되기 때문.
> 예외: **기계 파싱 규약**(예: 프로필 라인의 ` — ` 구분자, signal-bridge RULES §4.1)은 데이터 형식이지 카피가 아니다. 규약 변경은 파서·RULES 동시 갱신이 전제.
> UI에 내부 문서 참조(`§0`·`D-016` 등)를 노출하지 않는다 — 근거는 문서에, 화면엔 결론만.

- **1차 해석 원칙**: 읽자마자 이해되는 문장만. 은유·중의·수사로 2차 해석을 요구하지 않는다.
- **em dash(—) 금지.** 필요하면 문장을 나누거나 쉼표·마침표로.
- **AI 상투 표현 금지**: "혁신적인"·"완벽한"·"~의 세계로" 류 과장 수식, 기계적 나열투, 불필요한 느낌표.
- **정확한 말을 쓴다. 쉬운 말을 억지로 만들지 않는다**(2026-07-30 도메인 소유자 지시로 교체된 규율).
  - 기준은 어휘의 난이도가 아니라 **2차 해석을 요구하는가**다. 억지로 만든 쉬운 말은 원래 뜻을 잃고 읽는 사람이 되짚게 만들어, 어려운 말보다 나쁘다.
  - ❌ **비유·별칭**: `지리 지문` · `홈(KR)` · `송곳 타겟팅` · `화이트스페이스`. 무엇을 가리키는지 한 번 더 생각해야 한다.
  - ❌ **압축 기호**: 제목의 `×` · `[대괄호 태그]` · 괄호 안에 밀어 넣은 경고문(`(열 간 비교 금지)`). 제목이 아니라 색인이 된다.
  - ✅ **가리키는 것을 그대로**: `팀별로 어느 나라 차트에 올랐나` · `한국 차트` · `아직 안 올라간 나라`.
  - ✅ **분야의 정식 명칭은 그대로 쓴다**(`스트림`·`순위`·`코호트`·`백분위`). 바꾸면 오히려 부정확해진다. 처음 나올 때 한 줄 정의를 붙이는 것은 §7.1의 `definition` 필드가 한다.
  - 경고·한계는 제목의 괄호가 아니라 **자기 자리**(`definition`·`reliability`·`notAnswered`)로 보낸다.
- **명사형 종결 우선**: "준비 중"(○) "준비 중입니다"(✕). 행동 안내 등 서술형이 더 명확한 경우만 예외.
- **효율**: 한 문장 한 정보. 짧게 쓸 수 있으면 짧게.

### 6.2 `AI추론` 표기 규약 (D-039 · 도메인 소유자 결정)

> **자동 추론을 버리지 않고 태그해 보여준다.** 불변식이 금지하는 것은 **평결**이고, 꼬리표 달린 추론은 평결이 아니다 — 출처가 명시된 해석이며 사람이 채택·기각할 대상이다.
> 근거: FM 데이터 허브에서 자동 결론이 버려진 이유는 *결론이 있었기 때문이 아니라 근거가 안 보였기 때문*이다([`docs/REFERENCE-fm-data-hub.md`](docs/REFERENCE-fm-data-hub.md) §4).

| 규칙 | 내용 |
|---|---|
| **동반 4종 필수** | `basis`(어느 축·수치에서 나왔나) · `sample`(표본 n) · `confidence`(등급) · `limits`(못 보는 것). **하나라도 없으면 렌더하지 않는다** |
| **어법 금지** | 명령형("~하라") · 예측 단정("~할 것이다") · 인과 단정("~때문이다") |
| **어법 허용** | "~와 정합한다" · "~신호가 있다" · "~로 읽힌다" |
| **배치** | 추론은 항상 관측 **아래**. 같은 카드에 섞지 않고 배지로 구분 |
| **근거 열기** | 배지를 누르면 `basis`가 펼쳐져 원자료로 내려간다 |
| **배지 표기** | `AI추론` — 메탈릭 액센트 금지(§1 메탈릭은 브랜드 포인트 전용이며, 추론 배지가 화려하면 신뢰가 아니라 권위로 읽힌다) |

## 7. 차트 구현 스택 (D-040)

- **프리미티브: [visx](https://airbnb.io/visx)**(MIT · 필요한 `@visx/*`만 개별 설치) · **모션: `motion`**(MIT). 라이브러리가 룩을 가져오지 않는 것이 선택 이유다 — 스케일·축·형태·툴팁의 **수학만** 빌린다.
- 🔴 **라이브러리 테마·기본 색·기본 서체 사용 금지.** 전부 §3 토큰으로 덮는다. 완성 컴포넌트를 주는 라이브러리(Recharts·Nivo·ApexCharts)를 안 쓰는 이유가 이것이다.
- **`@visx/xychart` 등 고수준 추상은 쓰지 않는다** — 저수준(`shape`·`scale`·`axis`·`tooltip`·`brush`·`event`)만. 열린 툴팁·줌 이슈가 고수준 쪽에 몰려 있다.
- **dataviz 스킬이 상위 규칙이다**: 이중축 금지 · 카테고리 색 **고정 순서**(순환 금지) · **팔레트 검증기 통과**(§3 절차) · 시퀀셜=단일 색조 / 다이버징=두 색조+중립 중간 · 범례는 2계열 이상에서 항상 + 선택적 직접 라벨 · 표 뷰 제공 · 텍스처 대안. **모든 차트를 안티패턴 카탈로그에 대조한다.**
- **인터랙션 기본 탑재**: 선·면에 크로스헤어+툴팁, 막대·점·셀에 마크별 툴팁. 히트 타깃은 마크보다 크게. 필터는 차트 위 한 줄.
- **모션 상한은 §5 그대로.** ⚠ 공식 `frontend-design` 스킬의 경고를 규칙으로 승계: **"과한 애니메이션은 디자인이 AI 생성처럼 읽히게 만든다."** 모션은 상태 변화를 설명할 때만.
- **ECharts는 도입하지 않는다**(현 314레코드에 ~300KB의 근거 없음 + 자체 테마가 §3과 경합). **재검토 조건**: 단일 차트 10만 점 초과, 또는 지도·3D 요구 발생.
- 차트는 **클라이언트 컴포넌트**로 두고 정적 export를 유지한다(ARCHITECTURE 정적 우선).

## 7.1 리포트 계약의 시각화 필드 (D-041 · 요구사항 R1~R8의 계약 층)

> 요구사항 정본은 [`docs/REFERENCE-fm-data-hub.md`](docs/REFERENCE-fm-data-hub.md) §5.1. 여기는 **그것을 무엇으로 실어 보내는가**다.
> 스키마 정본은 `packages/report-schema/report.schema.json`, 읽는 쪽은 `apps/dashboard/lib/report.ts` — 둘은 짝으로 갱신한다(AGENTS §0).

| 필드 | 요구 | 규칙 |
|---|---|---|
| `summary` | R2 | **배열이 아니라 객체 하나.** 첫 화면에 도형 두 개를 놓을 수 없게 구조로 막는다. 여기 실은 차트는 `charts`에 중복하지 않는다 |
| `questions[]` | R1 | `{q, chartId}` · 상한 4개. `chartId`는 실재하는 차트 id여야 한다(끊긴 앵커는 검사기가 잡는다) |
| `charts[].question` | R3 | **답할 질문이 없으면 그 차트를 뺀다.** 이 필드를 비워 두는 선택지는 없다 |
| `charts[].definition` · `metrics[].definition` | R5 | 화면에서 인라인 펼침. 원장(RULES.md)으로 보내는 링크가 아니다 |
| `reliability` (최상위 + 차트별) | R8 | 차트별 값이 최상위를 **필드 단위로 덮는다**. 렌더 위치는 플롯 **위** — 아래로 내리면 각주가 되고 각주는 안 읽힌다 |
| `notAnswered[]` | R7 | 이 화면이 **답하지 않는 질문**. 한계 서술(`insights`)과 다르다 |
| `inferences[]` | R4 | 5필드 전부 필수(`text` + 동반 4종). 어법 금지 항목은 검사기가 CI에서 본다. 표기 규약은 §6.2 |
| `charts[].id` | R1·R4 | 앵커 대상. **제목 문자열로 앵커를 잡지 않는다** — 문구를 다듬는 순간 링크가 조용히 끊긴다 |

- **채택은 모듈 단위다.** 스키마는 필드를 선택으로 열어 두고, 탭이 실제로 완성될 때 `scripts/validate_report_data.py`의 `ADOPTED_MODULES`에 한 줄 추가한다. 그 순간부터 위 표가 그 모듈의 하드 게이트가 된다. **목록에 없는 모듈은 게이트를 우회하는 것이 아니라 아직 채택하지 않은 것이다** — 목록이 줄지 않고 멈추면 그게 신호다.
- 현재 채택: **6모듈 전부**(sonic-profile 파일럿 → chart-history → fandom-pulse · yt-pulse · genre-impulse · signal-bridge, 2026-07-30). 새 모듈은 `ADOPTED_MODULES`에 한 줄을 추가하는 것으로 계약에 들어온다.

## 7.1.1 구획 — 한 화면에 최대 3 (D-043 · 도메인 소유자 지시)

> *"너무 많은 정보를 보여주지 않는다. 페이지가 극도로 길어지더라도. 하나의 채널에는 최대 3가지의 정보만 담는다."*

- **구획은 질문 단위로 가른다.** `sections[]`의 머리글은 라벨이 아니라 **질문**이다. "플랫폼"이 아니라 "네 플랫폼이 같은 곡을 올리고 있나?"여야 읽는 사람이 어디로 갈지 정할 수 있다.
- **한 구획의 차트는 3개까지.** 상한은 `scripts/validate_report_data.py`의 `MAX_CHARTS_PER_SECTION`이 CI에서 센다. 리뷰어의 눈에 맡기면 하나씩 늘어나 결국 예전 상태로 돌아간다. **차트를 하나 더 얹으려면 그것이 어느 질문에 답하는지를 먼저 답해야 한다** — 새 구획을 만들거나, 답이 겹치는 차트를 뺀다.
- **지표도 구획에 나눠 넣는다.** 타일 13개를 첫 화면에 쌓지 않는다. 지표는 자기 질문 옆에 있을 때만 읽힌다.
- **한 번에 한 구획만 렌더한다.** 그래서 다른 구획의 차트는 DOM에 없다 — 질문 앵커는 구획을 **먼저 열고** 스크롤해야 하고(`onBeforeJump`), 탭 스모크도 **구획마다** 돌아야 한다. 첫 구획만 검사하면 나머지 구획의 렌더 결함이 그대로 통과한다.
- 구획을 선언하지 않은 리포트는 지금까지처럼 한 줄로 렌더된다(하위 호환).

## 7.1.2 진입부의 배치 규율 (실측)

- **같은 카드를 나란히 놓지 않는다.** 질문 목록과 요약 도형을 둘 다 글래스 카드로 두면 표면당 강조가 둘이 되어 위계가 사라지고(§1 미니멀리즘), 내용이 짧은 쪽 아래에 빈 상자가 남는다. **질문 목록은 본문 텍스트로, 카드는 도형에만.**
- 높이가 다른 두 칸을 위쪽 정렬하면 짧은 쪽 아래가 빈 구멍으로 읽힌다. 카드 크롬을 걷어내면 같은 공백이 **여백**이 된다. 상자를 없애는 것이 채우는 것보다 먼저다.
- 요약에 붙은 추론은 카드 밖 전폭으로 내린다. 카드 안에 두면 요약 카드만 길어져 균형이 깨진다(관측 아래라는 §6.2 배치는 그대로 지켜진다).

## 7.2 요약 도형(R2)의 규율

- **절대값을 그리지 않는다.** 스케일이 다른 축을 한 폴리곤에 얹으면 그림이 값이 아니라 **스케일**을 보여준다(실측: 축별 중앙값이 0.015~0.557로 자릿수가 다름). 공통 스케일(백분위 등)로 옮기고 무엇으로 정규화했는지 `scaleNote`에 싣는다.
- **기준 링은 필수 동반자이고 크롬이다.** 비교 대상이 도형 안에 그려져야 "선 밖이 코호트보다 높은 쪽"이라는 판독 규칙이 성립한다. 시리즈가 아니므로 색을 쓰지 않고 범례를 요구하지 않는다.
  - 🔴 **기준 링에 직접 라벨을 붙이고 격자 링과 겹치지 않게 한다.** 겹치면 격자 중 하나로 읽혀 이 도형의 유일한 판독 규칙이 사라진다(2026-07-30 육안 검사에서 실제로 발생).
- **결측 축에서 폴리곤을 잇지 않는다**(R6). 0으로 이으면 도형이 "이 축에서 분포 최하위"라고 거짓말한다. 결측이 있으면 면도 채우지 않는다(면적이 거짓이 된다).

## 7.3 색 토큰 상한과 스몰 멀티플 (실측 결함에서 온 규칙)

- 카테고리 토큰은 **3개**(`--series`·`--series2`·`--series3`)이고 **순환하지 않는다**. 시리즈가 3개를 넘으면 색을 재사용하는 대신 **스몰 멀티플로 접는다**(패널당 시리즈 1개 = 슬롯 1 = 범례 불필요).
- 근거: 2026-07-30 실측에서 6시리즈 라인차트가 토큰을 순환해 `펄스 명료도`와 `스펙트럼 평탄도`가 **같은 보라색**으로 그려졌고, 범례를 봐도 어느 선이 무엇인지 가릴 수 없었다. 정적 게이트 4종·탭 스모크 전부 통과한 상태였다 — **색 순환은 코드가 아니라 눈으로만 잡힌다.**
- 패널은 **축척을 공유**하고 그 사실을 화면에 적는다. 공유하지 않으면 잡음이 트렌드로 확대되고, 공유한다고 말하지 않으면 패널끼리 비교해도 되는지 알 수 없다.

## 7.4 결측 마크는 플롯 밖에 둔다 (R6의 렌더 층)

- 결측 표시를 플롯 **안**에 찍으면 그 자체가 값 위치로 읽힌다. 바닥(y=0)에 점을 놓았더니 실값이 0.01인 축에서 관측값과 구별되지 않았다(2026-07-30 육안 검사). **축 아래 밴드의 마크는 값이 될 수 없다** — 거기 둔다.
- 결측 개수를 차트가 스스로 말한다. 끊긴 선만으로는 "왜 끊겼나"를 전달하지 못하고, 눈치채지 못하면 결측이 0으로 읽히던 예전 상태로 돌아간다.

## 7.5 값의 방향 (실측 결함에서 온 규칙 · 2026-07-30 2차)

> 전부 정적 게이트 4종과 탭 스모크를 **통과한 상태**에서 육안으로만 잡힌 것들이다. §7.3·§7.4와 같은 부류이고, 공통점은 **그림의 방향과 주장의 방향이 어긋난 것**이다.

- **순서만 있는 값을 길이에 싣지 않는다.** 순위(1위가 강함)·부호(방향)는 크기가 아니다.
  - 부호: 선행 `+62`와 지연 `−9`를 한 막대에 실었더니 렌더러가 음수를 최소 폭으로 클램프해 **차트 선행 44팀이 전부 같은 길이의 짧은 막대**가 됐다. 방향은 **차트를 가르는 데** 쓰고 길이에는 크기만 싣는다.
  - 역방향 축: "낮을수록 부합"인 축을 막대로 그리면 **긴 막대가 덜 부합**을 뜻한다. 같은 단위의 축이 여럿이면 격자로 옮긴다(값의 위치를 보여줄 뿐 길이로 주장하지 않는다).
- **격자의 색 방향은 리포트가 정한다**(`scale: "rank" | "value"`). 프리미티브의 기본은 순위(작을수록 진함)라, 백분위를 그냥 실으면 낮은 값이 가장 진하게 칠해지고 범례가 그것을 "상위"라고 부른다. 범례 문구도 payload에서 온다.
- **관측하지 않은 구간을 0으로 잇지 않는다**(R6의 선 버전). 수집 창이 다른 두 시리즈를 합집합 날짜에 얹으면 한쪽만 있는 구간이 생기는데, 거기 0을 채우면 화면이 "그때는 없었다"고 말한다 — 사실은 "그때는 아직 수집하지 않았다"다. `null`로 두고 선을 끊고, **끊긴 개수를 차트가 스스로 말한다.**
- **자를 때는 자른다고 적는다.** 264행짜리 막대는 읽는 것이 아니라 스크롤하는 것이라 상위 N행만 그리되, 말없이 자르면 화면이 전부를 보여준 것처럼 읽힌다. 상한과 전체 수를 신뢰도 라인에 싣는다.
- **저장 키를 화면에 찍지 않는다.** `organic_ratio`·`over_unity_ratio` 같은 이름이 축 라벨·툴팁·인사이트로 새어 나가면 데이터 키가 그대로 노출된다. 표기는 리포트가 함께 싣고(`axisLabels` 등), 같은 축은 **탭이 달라도 같은 이름**으로 부른다.

## 8. 하드 게이트 체크리스트 (클라이언트 대면 변경 전)

- [ ] 색·폰트·그라디언트 하드코딩 없음 — 토큰 참조만
- [ ] 라이트/다크 양쪽에서 확인
- [ ] 증거 레이어(차트 플롯·표·본문) 위에 오버레이·그라디언트 없음(§4)
- [ ] 문구에 단정·과대주장 없음, 카피 스타일 준수(§6.1)
- [ ] `lint`·`typecheck`·`build` 통과
- [ ] **팔레트 검증기 통과**(색을 건드렸다면) — 라이트/다크 각각, 6검사 전부 PASS
- [ ] dataviz **안티패턴 카탈로그 대조**(§7)
- [ ] 추론을 렌더한다면 **동반 4종**(`basis`·`sample`·`confidence`·`limits`) 존재 + 어법 금지 항목 없음(§6.2)
- [ ] **차트 데이터 계약 검사기 통과** — `python scripts/validate_report_data.py` + `--selftest`(게이트 자체 검사). 계약을 채택한 모듈은 R1~R8이 여기서 강제된다(§7.1)
- [ ] **시리즈가 3개를 넘는데 색을 순환시키지 않았는지** — 넘으면 스몰 멀티플(§7.3). 이것은 정적 게이트가 못 보므로 렌더를 눈으로 확인한다
- [ ] **결측이 값처럼 보이지 않는지** — 결측 마크는 플롯 밖(§7.4), 선은 끊김, 개수 표기 존재
- [ ] **값의 방향이 그림의 방향과 맞는지**(§7.5) — 부호·순위를 길이에 싣지 않았는지 · 격자에 `scale`을 실었는지 · 관측 없는 구간을 0으로 잇지 않았는지 · 자른 행 수를 적었는지 · 저장 키가 화면에 나오지 않는지
- [ ] **탭 스모크 통과** — `cd apps/dashboard && npm run smoke:tabs`(dev 실행 중일 때). 전 탭 × 라이트/다크 × **구획마다** 콘솔 에러 0 · 렌더 확인 · **탭 전환이 실제로 되는지**(`aria-selected`). 정적 게이트 3종이 다 통과하고도 대시보드가 죽어 있던 사례(2026-07-30)의 답이다
- [ ] **`build`는 dev를 끄고 돌린다.** `distDir`를 갈아 끼우는 우회는 통하지 않는다(2026-07-30 실측: `.next`가 프로덕션 산출물로 덮여 dev가 **하이드레이션 없는 HTML**을 내보냈다). 그 상태는 화면이 멀쩡히 그려지는데 아무것도 눌리지 않아 눈으로도 늦게 발견된다. 3000을 다른 프로젝트가 점유 중이면 `AI_DEV_PORTS=3100`으로 검사 범위만 좁힌다

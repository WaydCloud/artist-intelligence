# 2026-07-31 — A2 본편: 동시대 코호트 실측 → 검출 규칙 3 → 커버리지 3/10

> 재개점은 [`../HANDOFF.md`](../HANDOFF.md). 이 파일은 이 세션의 이력 스냅샷이다.

## 한 것

1. **콜드 실행 미도착 원인 확정**: 07-30 daily(09:00)는 v4 커밋(12:34)보다 먼저 돌아 v3 산출이었다. 콜드 실행은 07-31 09:00 실행이 된다.
2. **동시대 코호트 두 프레임 구축**:
   - US = Billboard Hot 100 2021-10-02 톱100 → 특질 98/100 (rhythm v4).
   - KR = **Wayback 멜론 주간**(신규 `scripts/melon_wayback_ingest.py` — CDX 최근접 탐색·거리 병기·파생 사실만). 앵커 정확 주차 스냅샷은 없어 +23일(주간 2021-10-18~24) 채택, 특질 98/100. 임영웅 "잊었니" 1곡은 동일 track_id 해석으로 정체성 병합에 흡수(모듈의 의도된 동작).
3. **사전 등록 커밋(`11b63be`)**: 원장 predicted_axes(산문) → 기계 축 **측정 번역 표**를 백분위 산출 전에 CASEBOOK에 적고 커밋. 결과를 보고 기준을 고르지 않는 순서를 git이 증언한다.
4. **케이스 곡 v4 재취득**(101/110): A2.1 미해석 6곡 중 3곡 해소(money machine·Doja·Ride on Time — US 스토어), 가시나·Swoosh Flow 해석. 잔여 = 저지클럽 클럽 원형 8곡(iTunes 부재) + astrid.
5. **백분위 실측**(신규 `scripts/a2_signature_compare.py` — `_percentile`·`derive_all` 공유, 리듬 버전 가드): 판정 전건 CASEBOOK §A2 본편. 압축하면 —
   - **적중**: amapiano(여백·셰이커 전곡 상위) · ukg 원형(스윙·템포) · moombahton KR형(명도↑·대비↓) · njs 수용형 타격감 중앙화.
   - **실패 기록**: brightness류 반복 없이도 — ukg syncopation·ioi 중앙 · moombahton 108BPM 감속 중앙 · njs attack 저하 예측 반대 · citypop 판별력 없음.
   - **서명 변형 공통 패턴(가설)**: 원형의 극단 축 중 하나는 유지, 하나는 완화 — 모니터는 **유지되는 축**을 봐야 한다.
   - **hyperpop 재검**: organic↓·flatness↑ 동시대에서도 성립, **over_unity는 프레임 의존**(동시대 중앙 P66 — A2.1의 P80~100은 2026 라이브 코호트 의존). 규칙 한계에 기록, 도메인 소유자 보고거리.
   - easy-listening은 **프레임 부적합**(2021 앵커 ≠ 2023 주류화) — 판정 유보.
6. **베이스라인 실측이 등재를 뒤집었다**: amapiano 2축 조합은 정답지 **7/7**인데 코호트 베이스라인 **20~21%**(세 코호트 일관 — 축 양의 상관). **등재 철회**(RULES §2.2). 교훈: *정답지가 극단에 있다는 사실만으로 규칙이 되지 않는다 — 그 극단이 얼마나 붐비는지 같이 재라.*
7. **genre-impulse v1.2 — 규칙 2종 등재** (커버리지 1 → 3/10):
   - `ukg-origin-shuffle`(§2.1.2 · high_all 스윙·템포 — 원형 진입 감지 · 재현 3/6, 그중 1곡은 어쿠스틱 버전 오해석 · 베이스라인 7~10%).
   - `moombahton-kr-tropical`(§2.1.3 · 대비 하위+명도 상위 — 청량 변형 재등장 감지 · 재현 5/7 · 베이스라인 5~8%).
   - 엔진 high_all 조합 + 규칙별 극단 축 정렬 + 부분 결측 트랙 튜너 잔류. 대시보드 튜너 동기화(`highAll` — **빈 `highAny`의 `[].some()`=false 함정**을 CLI(`any([])`)와 양쪽에서 밟을 뻔했다).
   - 커버리지 요약 4구획: 확정 3 · **실측·규칙 미확정 4(신설 — "서명 없음"이라 부르면 거짓)** · 작업 중 1 · 없음 2.
8. **육안에서만 잡힌 결함 3건** (정적 게이트·스모크 전부 초록 상태): ① 격자에 규칙 귀속 없음 — 여러 규칙에 걸린 곡(캐치 캐치 ×3)이 중복 행 버그처럼 읽힘 → 행 접두 ② 전체 규칙 id 접두가 곡명을 잘라먹음 → short 표시명 ③ **매치(곡×규칙 쌍) 27건을 "27곡"으로 표기** — 실제 19곡. 타일·인사이트·튜너 요약 전부 유일 곡 수로.

## 게이트

selftest 17/17(신규 §3.18~21) · ruff · pyright 0 · `validate_report_data` CLEAN(+`--selftest` 90/90) · tsc · next lint 0 · `smoke:tabs` 12/12 · genre-impulse 탭 라이트/다크 구획별 스크린샷 육안.

## 커밋

`11b63be`(사전 등록) → `a8529e8`(실측·원장) → `d897935`(규칙 등재·v1.2) → HANDOFF.

## 미결 (다음 세션)

- **콜드 실행 실측**(07-31 09:00 daily가 v4 첫 실행) → H3·H4 게이트(RULES §3.1.5.4).
- 대기 목록: 빌보드 궤적 → 임펄스 원장 · 저지클럽 미해석 8곡 대체 · 케이스별 동시대 앵커 확장(easy-listening 2023 등 — US는 즉시, KR은 Wayback 커버리지 병목).
- 도메인 소유자 보고: hyperpop 규칙 over_unity 갈래의 프레임 의존 · 신규 규칙 2종의 임계(관습값 P20/P80) 검토.

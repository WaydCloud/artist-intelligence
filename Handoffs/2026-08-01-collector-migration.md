# 2026-08-01 (저녁) · 수집 이전 — 결정 넷을 받고 러너를 세웠다. 옮겨지지 않는 것 셋이 나왔다

> 앞 세션은 [`2026-08-01-backup-live-and-ip-verdict.md`](2026-08-01-backup-live-and-ip-verdict.md)(1층 백업 가동 · 되돌리기 · IP 판정).
> 결정: **[`docs/DECISIONS.md`](../docs/DECISIONS.md) D-059** · 판단 자료: [`docs/DRAFT-saas-platform.md`](../docs/DRAFT-saas-platform.md) §2.2.

## 한 줄

앞 세션이 "결정 넷을 받고 착수하라"고 적어 둔 자리에서 시작해 넷을 받고 워크플로를 세웠다. **그런데 이전의 실제 내용은 코드를 옮기는 일이 아니었다** — 코드가 딛고 서 있던 것들 중 **옮겨지지 않는 것 셋**을 찾아내는 일이었다.

## 1. 결정 넷 (도메인 소유자 · 전부 엔지니어 권고안)

| # | 결정 | 핵심 이유 |
|---|---|---|
| 1 | **파생 3종을 전부 러너에서** | 32초를 1년 뒤 문제로 미리 최적화하는 것은 오버핏. 아플 때 옮긴다 |
| 2 | **이전이 멜론보다 먼저** | 멜론 MCP는 헤드리스 불가 — 먼저 복귀시키면 이전 설계가 예외를 전제로 시작한다 |
| 3 | **`windows-latest`** | 재작성 0. PUBLIC 레포라 분(minute)이 무제한 무료여서 2배 가중이 비용이 아니다 |
| 4 | **`09:17` + 2시간 간격 4회** | 정각은 전 세계가 몰리는 최악 슬롯이고, 예약 워크플로는 밀리는 게 아니라 **건너뛴다** |

결정 1이 러너 설계의 모양을 정했다. 파생 3종이 **1층 전체를 읽으므로**(전 날짜×전 시장 · 전체 glob · 전 스냅샷) 러너가 복원하는 것은 "오늘 것"이 아니라 **전 이력**이다. 그래서 순서가 `복원 → 수집 → 백업`이 됐고, `backup_live.py --restore`(D-058 ⑩)가 그대로 부트스트랩이 됐다.

## 2. 세운 것

- **[`.github/workflows/daily-collect.yml`](../.github/workflows/daily-collect.yml)** — 15스텝. `shell: powershell`(5.1)로 **같은 `daily_collect.ps1`을 그대로** 돌린다. 결정 3이 산 것이 바로 이 파일을 손대지 않는 것이다.
- **[`requirements-daily.txt`](../requirements-daily.txt)** — 버전 고정. 이유는 재현성이 아니라 **엔진 지문**이다(아래 §4).
- **삭제**: `scripts/ip_probe.py` · `.github/workflows/ip-probe.yml`(목적 완수).

### 게이트를 앞에 세 개 뒀다

셋 다 같은 실패를 막는다 — **실패가 빨간 X가 아니라 그럴듯한 산출물로 나타나는 것.**

1. **시크릿 4/4** — 없이 진행하면 러너는 빈 `data/live`로 시작해 "오늘 하루치만 있는" 시리즈를 만들고 그것이 리포트가 된다.
2. **복원된 트리에 차트 날짜가 2개 이상인지** — `restore`는 원격이 텅 비었을 때만 실패하고 "일부만 왔다"는 초록으로 끝난다.
3. **되밀기 직전 스키마 + `validate_report_data.py`** — `GITHUB_TOKEN`으로 민 커밋은 워크플로를 **다시 트리거하지 않으므로**(재귀 방지) `ci.yml`이 러너의 리포트를 보지 못한다. 게이트를 여기 두지 않으면 계약 위반이 아무에게도 안 걸린 채 대시보드로 간다.

## 3. 🔴 옮겨지지 않는 것 셋

이번 세션의 실질이다. 셋 다 "코드는 그대로인데 코드가 딛고 있던 것이 안 따라오는" 형태다.

### ① 콘솔 코드페이지 — 이전 첫날 터졌을 자리 (서른셋째 건)

모듈 CLI **43줄**이 한글을 stdout으로 찍는다. 인코딩 짝(`PYTHONIOENCODING` + `[Console]::OutputEncoding`)은 스크립트 **중간(sonic 레그)**에서야 걸리고 있었고, 그 앞의 레그들(chart analyze · social merge · yt)은 **로컬 콘솔이 cp949라 우연히** 되던 것이다.

```
PYTHONIOENCODING=cp1252  python -c "print('한글')"   → UnicodeEncodeError · exit 1
PYTHONIOENCODING=utf-8 PYTHONUTF8=1  같은 명령        → exit 0
```

러너의 ANSI 코드페이지는 cp949가 아니다. **`chart-history` 라이브 리포트 레그부터 죽는다.**

고친 방식은 D-058 ⑤와 같다 — **사람이 지키지 않아도 되게.** 짝을 스크립트 맨 위로 올려 모든 레그보다 먼저 한 번 걸고, 아래 어느 레그도 다시 묻지 않는다. 워크플로 잡 전체에도 건다(스크립트를 거치지 않는 스텝이 있으므로).

> 🔺 **로컬에서 초록인 것은 "맞다"가 아니라 "이 환경에서 맞다"이다.** 이건 육안으로도 실행으로도 안 보인다(로컬에서는 영원히 초록이다). **환경 가정을 목록으로 훑어야** 보인다 — 앞의 서른두 건과 발견 방식이 다르다.

### ② 중단 레버 — `data/live/PAUSE`는 러너에 오지 않는다

gitignore인 `data/live` 안에 있고 백업의 `_EXCLUDED`("가드 파일")다. 즉 문서가 "중단: `data/live/PAUSE`"라고 적어 둔 레버가 **이전 다음 날부터 아무 일도 하지 않는다.** 러너 몫의 레버를 워크플로 첫 스텝에 뒀다(변수 `PAUSE=1`).

### ③ 저장소의 단일 키 — 병행 운전이 대조 대상을 오염시킨다

앞 세션이 남긴 경고는 **돈**이었다("유료 레그가 두 번 돌면 하루 $6"). 짜면서 보니 더 조용한 쪽이 있었다: 로컬과 러너가 같은 버킷의 **같은 키**를 쓴다. `state/<날짜>.tar.gz`와 `logs/daily.tar.gz`는 키가 하나뿐이라 **나중에 올린 쪽이 앞의 것을 지운다.**

- 로컬이 `done=true`로 닫은 상태를 러너가 덮거나(그 반대거나) → 재개 기계장치가 서로를 오해한다.
- 유료 지출 감사 로그가 반쪽만 남는다.

**대조하려고 켠 병행 운전이 대조 대상을 오염시킨다.** 그래서 병행 기간의 러너는 **복원만 하고 아무것도 올리지 않는다.**

새 플래그를 만들지 않았다 — 수집 스텝에 `SUPABASE_*`를 **빈 값으로 덮으면** 스크립트가 백업 레그를 호출조차 하지 않는다. D-058 ④("설정이 없으면 아무것도 나가지 않는다")가 그대로 레버가 된다.

## 4. 엔진 지문을 고정한 이유

sonic 레코드는 `engine_version`(= librosa 버전)을 함께 싣고, 게이트는 지문이 어긋난 값을 **한 분포에 넣기를 거부한다**(D-037·D-038). 러너가 다른 버전으로 재면 그날 값은 음악이 아니라 **측정 조건 차이**를 재고, 백분위가 조용히 그것을 반영한다.

디코더(`av`)·리샘플러(`soxr`)·수치 스택도 핀에 들었다 — 지문에 안 실릴 뿐 값은 바꾼다.

🔴 올릴 때는 **로컬을 먼저 올리고 같은 값을 반영한다.** 반대로 하면 러너가 먼저 다른 자로 재고, 1층은 재취득 불가라 그 하루는 되돌릴 수 없다.

## 4.5 🔴 첫 실행이 서른넷째 건을 냈다 — 가드가 열린 쪽으로 실패했다

run [`30683288722`](https://github.com/WaydCloud/artist-intelligence/actions/runs/30683288722) · 3분 27초 · 초록.

켜는 조건을 `vars.AI_DRYRUN == '0'`으로 짰다. **GitHub 표현식의 `==`는 피연산자를 숫자로 캐스팅한다** — 미설정 변수는 `''` → 0 이고 `'0'` → 0 이라 **변수를 설정하지 않았을 때 참**이 된다. "미설정=꺼짐"으로 설계한 기본값이 정확히 반대로 돌아 `AI_DRYRUN=0`·`LIVE_MODE=1`(유료 + 1층 쓰기)로 실행됐다.

**피해 0.** 오늘(08-01)이 로컬에서 이미 완주라 스크립트가 재개 상태를 보고 no-op으로 나갔다.

```
2026-08-01T04:09:58 | === daily_collect skip (2026-08-01) -- already completed at 2026-08-01T10:00:48, no-op ===
backup uploaded 1 object(s) 0.00MB, skipped 53 unchanged (bucket live-raw)
verify: 54 local group(s), 54 in manifest, 54 remote object(s) -- verify OK
```

올라간 1객체는 `logs/daily.tar.gz`(no-op 한 줄이 붙은 것)다. **하루가 닫혀 있지 않았다면 "읽기 전용 시험"이 $3을 쓰고 1층에 썼다.**

고친 것 둘:
1. **켜는 조건은 숫자로 캐스팅될 수 없는 문자열** — `vars.RUNNER_MODE == 'live'`.
2. **멈추는 레버는 반대 방향으로 안전해야 한다** — `PAUSE != ''`(비어 있지 않은 아무 값에나 선다). `== '1'`로 두면 `PAUSE=stop`이 조용히 무시된다.

> 🔺 그리고 이것이 보인 유일한 자리는 스텝 로그의 `AI_DRYRUN: 0` 한 줄이었다 — **읽을 이유가 없는 자리다.** 그래서 preflight가 이제 **값이 아니라 결과를 말로** 찍는다(`MODE = LIVE -- paid social WILL run ... layer-1 WILL be written`). 잘못된 설정이 **조용한 성공**으로 나타나지 않게 하는 것이 §2의 게이트 셋과 같은 규율이다.
>
> **배운 것**: 설정 언어의 비교 연산자가 우리 언어와 같은 뜻이라고 가정하지 않는다. **기본값의 방향은 주석이 아니라 실행 로그로 확인해야 한다.**

### 그 실행이 실제로 증명한 것

| | 결과 |
|---|---|
| 복원 | **1,627파일 / 54아카이브 / 14.33MB** — 로컬 실측과 동일 |
| 원격 대조 | `verify OK` 54/54/54 |
| 파이썬 | 3.14.6 (로컬 3.14.5와 같은 마이너) |
| 의존성 설치 | ~95초 (pip 캐시 콜드) |

⏳ **아직 안 재진 것**: 수집·파생 레그 · 콜드 캐시 비용(모델 ~500MB) · sonic 레그가 러너에서 도는지. 다음 실행은 **하루가 열려 있는 시각**에 돌려야 뜻이 있다.

## 5. 알고 들어가는 것

- **아직 재지 않은 표면 둘**: `cloud.cp.jku.at`(beat_this 체크포인트 81MB) · `essentia.upf.edu`(ONNX 태거). ip-probe는 스크레이프 소스만 쟀다. 둘 다 캐시하지만 **첫날은 콜드**다. 막히면 sonic 레그만 죽고 나머지는 돌며 하루는 열린 채 재시도된다.
- **러너 5회째 크론은 무의미하다** — 스크립트가 `$maxAttempts = 4`에서 결손을 확정한다. 네 번으로 맞춰 뒀다.

## 6. 게이트

`ruff`(modules+scripts) 통과 · `validate_report_data.py --selftest` **90/90** · 본검사 CLEAN · `backup_live.py --selftest` 0건 · `daily_collect.ps1` 실행 확인(완주일이라 no-op으로 정상 종료 — PowerShell은 실행 전에 파일 전체를 파싱하므로 편집이 문법을 깨지 않았다는 증거이기도 하다).

## 7. 다음

`HANDOFF.md` 참조. **㉯ 시크릿 넷 → ㉰ dispatch 1회(무료·읽기 전용) → ㉱ 병행 하루 → ㉲ 변수 둘 켜고 같은 날 로컬 크론 해제.**

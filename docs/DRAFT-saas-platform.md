# DRAFT — SaaS 플랫폼 전환 · 프리미엄 프론트엔드 (2026-07-31 착수 지시 · 설계 대기)

> **지시**(도메인 소유자, 2026-07-31): 이것을 **SaaS·플랫폼**으로 만들고, **스크롤리텔링 등 SOTA 수준의 프리미엄 프론트엔드**를 갖춘다.
>
> 이 문서는 그 전환의 **착수 지점**이다. 결정된 것이 아니라 **무엇을 결정해야 하는지**를 적는다. 지금 코드를 쓰기 시작하면 안 되는 이유가 아래 §2에 있다.

---

## 1. 이 전환이 딛고 설 수 있는 것 (이미 있는 자산)

전환을 처음부터 시작하지 않아도 되는 이유들이다. 과소평가하면 다시 만들게 된다.

| 자산 | 왜 값이 나가나 |
|---|---|
| **모듈 6종 × 공유 계약** | `모듈 CLI → 스키마 유효 report.json → 대시보드`가 관통한다. 프런트를 갈아엎어도 **데이터 계약은 그대로 쓴다** |
| **시각화 계약 R1~R8** | 요약 도형·질문 앵커·신뢰도·추론 동반 4종이 **스키마로 강제**된다(`validate_report_data.py`). 새 프런트가 이걸 버리면 규율이 문서로 되돌아간다 |
| **디자인 하네스** | `DESIGN.md`에 브랜드·토큰·표면 위계·모션 규율이 정본으로 있고, **눈으로만 잡히는 결함 24건**의 교훈이 규칙으로 박혀 있다. 프리미엄 프런트에서 가장 비싼 것이 이 축적이다 |
| **인터랙션·스모크 게이트** | `smoke:tabs`가 전 탭 × 구획 × 라이트/다크 × 인터랙션 × 질문 앵커를 실제로 눌러 본다 |
| **LABS 선례**(D-052) | **계약 밖 표면을 계약을 깨지 않고 붙이는 법**이 이미 있다. 새 표면은 이 패턴을 복제한다 |
| **기준 원장 규율** | 임계값이 코드에 은닉되지 않고 튜너로 노출된다. 유료 고객에게 **반박 가능성**을 파는 제품에서 이건 기능이다 |
| **전향 실증 데이터** | chart 15일(51시장) · social 95파일 · yt 8일 · sonic 4일. 관측이 **매일 쌓이는 중** |

---

## 2. 🔴 먼저 풀어야 하는 것 — 코드보다 결정이 앞선다

### 2.1 아키텍처 불변식이 정면으로 걸린다

`AGENTS.md` §3은 **"임의로 라이브 백엔드·서버·인증·DB 추가 금지"**다. SaaS는 그 셋을 전부 요구한다. 이건 규칙 위반이 아니라 **규칙을 바꿀 결정**이며, 근거를 새 결정으로 남기면 된다(D-003이 이미 "라이브 분석이 필요해지면 Python 비동기 워커(Modal 1순위)"로 길을 열어 두었다).

> ✅ **미결이던 논의가 이 지시로 풀린다.** `HANDOFF.md`의 🟡 "상시 서버"는 **서버가 정당해지는 조건 3개**를 적어 두고 "아직 셋 다 아니다"라고 했다: ① 여러 사람이 쓴다 ② 온디맨드 분석이 필요하다 ③ JSON-in-git이 규모를 넘는다. **SaaS는 셋을 동시에 참으로 만든다.** 그러므로 이 전환의 첫 결정은 "서버를 둘 것인가"가 아니라 **"어떤 서버를, 무엇까지"**다.

### 2.2 🔴 수집이 이 PC 한 대에 묶여 있다 (가장 실질적인 병목)

지금 모든 관측은 **Windows Task Scheduler + 이 PC**에서 나온다. SaaS는 그 위에 세울 수 없다.

- **`data/live/`는 gitignore다.** 관측 이력이 git 밖에 있고 **이 PC에만** 있다(65MB). 고객에게 파는 시계열의 원본이 백업 없는 로컬 디스크에 있다는 뜻이다.
- **멜론 MCP 레그는 대화형 인증이라 헤드리스 불가.** 서버 이전 시 이 렌즈는 자동으로 따라오지 않는다.
- 후보: GitHub Actions cron(서버·DB 없이 PC 의존 소멸, git이 곧 이력) → Modal 워커(D-003) → 상시 서버. **①이 실제 통증이므로 여기서 시작한다.**

#### 2.2.1 실측 (2026-08-01) — 걱정하던 셋은 작았고, 걸리는 것은 다른 데 있었다

> HANDOFF가 "유료 키·모델 캐시·멜론 MCP 레그가 각각 어떻게 되는지 실측이 먼저"라고 적어 둔 것에 대한 답이다. **셋 다 이전을 막지 않는다.**

| 걱정하던 것 | 실측 | 그래서 |
|---|---|---|
| 유료 키 | 비밀값은 **둘뿐**이다: `APIFY_TOKEN`(유료 · 하루 $3 상한) · `YOUTUBE_API_KEY`(무료 할당량) | 시크릿 두 줄. 🔴 단 **예산 안전장치는 키가 아니라 원장에 있다** — 시도 원장이 실행 사이에 살아남지 못하면 재시도가 이미 산 태그를 다시 산다. 이건 아래 ①과 같은 문제다 |
| 모델 캐시 | `data/models/` = **28.6MB / 14파일** ONNX. 런타임에 essentia.upf.edu에서 받고 크기로 무결성을 본다(멱등). **torch·demucs는 daily 경로에 없다** — `--stems`를 줄 때만 걸리고 daily는 주지 않는다 | 30MB 내려받기. 캐시를 안 걸어도 된다. 무겁다고 알고 있던 500MB torch는 **daily와 무관**했다 |
| 멜론 MCP 레그 | `daily_collect.ps1`에 **없다**. 이름이 나오는 곳은 이전 마이그레이션 가드 한 줄뿐이다 | 이전을 막을 수 없다. 대화형 인증이라 헤드리스 불가인 것은 사실이나, **지금도 크론 밖**이라 옮기든 말든 상태가 같다 |

실제로 걸리는 것은 셋 다 아니다.

1. 🔴 **상태와 데이터가 실행보다 오래 살아야 한다.** 러너는 매번 새 디스크다. `data/live/`는 지금 **67.9MB / 1638파일**이고 **하루 약 5MB**씩 는다(대부분 차트 원본 HTML: 56.6MB / 1489파일). 여기에는 재개 상태와 **시도 원장**(돈)이 함께 있다. 후보마다 값이 다르다: git 커밋 = 연 1.8GB(§"JSON-in-git 규모 초과" 조건이 1년 안에 참이 된다) · Actions 캐시 = 10GB지만 **7일 미사용 축출**이라 누적 관측에 부적합 · 아티팩트 = 90일 보존이고 파일시스템이 아니다 · 외부 스토리지(S3/R2) = 새 의존성과 시크릿.
2. 🔴 **데이터센터 IP에서 소스가 열리는지는 여기서 잴 수 없다.** Kworb 스크레이프 · Apple RSS · iTunes 프리뷰 · Apify가 GitHub 러너 IP에서 지금처럼 응답하는지는 **실제로 한 번 돌려 봐야** 안다. 이전의 성패가 갈리는 지점이 여기이고, 국가별 차트를 걷는 레그라 지역 차단·레이트리밋의 표면이 넓다.
3. **크론의 시각은 보장이 아니다.** 예약 워크플로는 부하에 따라 밀리고 건너뛴다. 차트·소셜은 "오늘"만 서빙하므로 **거른 날은 영구 결손**이고, 지금의 09:00 + 2시간 간격 재시도는 그 성질에 맞춰 만든 것이다. 밀리는 크론 위에서 같은 보증을 어떻게 세울지가 이전의 설계 몫이다.
4. 스크립트가 Windows 전용이다(PS 5.1 · keep-awake P/Invoke · Task Scheduler · 역슬래시 경로). `windows-latest` 러너면 대부분 그대로 돌고 keep-awake만 무의미해진다. ubuntu로 가려면 다시 쓰는 일이다.
- 참고: 한 번 완주에 **17~25분**(2026-08-01 17분 · 07-31 25분 — sonic 콜드 실행 포함). 6시간 잡 상한 근처도 아니다.

**🔴 백업은 이 결정을 기다릴 이유가 없다.** 되돌릴 수 없는 것은 관측 원본(차트 HTML · 유료 소셜 스냅샷 · sonic 특징)뿐이고 파생물(`*_series.json` · `social_merged.json`)과 모델은 다시 만든다. 그 원본을 압축하면 **15.3MB**이고 **하루 증가분이 약 1.2MB**다. 수집을 어디서 돌릴지와 무관하게 지금 걸 수 있다.

### 2.3 🔴 스크롤리텔링과 책임소재 불변식의 긴장 — 이 전환의 핵심 설계 질문

`DOMAIN.md` §0: **판단은 책임질 인간에게, 도구는 증거에서 종료한다.** 그런데 **스크롤리텔링은 본질적으로 서사 형식**이다. 읽는 사람을 정해진 순서로 끌고 가 결론에 도착시키는 것이 그 장르의 힘이고, 그것을 그대로 쓰면 **도구가 평결을 배달하는 형태**가 된다 — 불변식이 정확히 금지하는 자리다.

**그러나 기각할 이유는 아니다.** 두 가지를 가르면 된다:

| 형태 | 예 | 판정 |
|---|---|---|
| ❌ **결론으로 데려가는 서사** | "스크롤하면 왜 이 팀이 뜰지 알게 됩니다" | 평결 배달. 금지 |
| ✅ **증거를 순서 지어 보이는 서사** | "이 축에서 이 팀이 어디 있는지, 무엇과 비교했는지, 무엇을 못 봤는지 차례로" | 불변식과 양립. **오히려 더 잘 지킨다** |

- 지금 대시보드는 **구획으로 질문을 가른다**(D-043). 스크롤리텔링은 그 질문들에 **순서**를 주는 것이고, 순서는 이미 정해져 있다(sonic-profile: 무엇인가 → 움직이나 → 진짜인가 → 우리 팀은 어디). **즉 자산이 이미 그 형태에 가깝다.**
- 🔴 **강제 스크롤·납치 스크롤 금지**는 접근성(§6)과 불변식 양쪽에서 온다. 읽는 사람이 **아무 때나 빠져나와 원본 수치로 갈 수 있어야** 한다.
- 🔴 **한계·불확실성이 서사에서 빠지면 안 된다.** 지금은 `reliability`·`notAnswered`가 1급 요소다. 스크롤리텔링에서 그것이 "마지막 슬라이드 각주"가 되는 순간 §5 과대주장 금지를 어긴다.

### 2.4 상업화가 새로 여는 위험 (지금까지 없던 것)

- 🔴 **저작권**: 한국 저작권법에 **TDM 면책이 없다**. 지금은 프리뷰 무보관·파생 분석만이라 회색이지만, **유료 제품**이 되면 그 회색의 성격이 달라진다. 법률 검토가 출시 전제다.
- 🔴 **태그 정확도가 영구 미측정**이다(D-054). 장르·악기 태그를 화면 중심에 두고 파는데 정확도를 재지 않기로 했다. 지금은 `참고(정확도 미측정)` 병기로 정직하지만, **유료 고객에게 그 병기가 어떻게 읽힐지는 별개 문제**다.
- **PII 게이트**는 이미 있다(수집 즉시 REJECT → quarantine). 고객 데이터·계정이 생기면 **범위가 넓어진다**(우리 고객의 개인정보).
- **가격·과금·다중 테넌시**는 도메인 소유자 결정이며 엔지니어가 정할 값이 아니다.

### 2.5 데이터 깊이가 아직 제품이 아니다

| 레일 | 깊이 | 실험 종료(2026-08-19) 기준 |
|---|---|---|
| chart | **15일** (51시장) | ~34일 |
| social | 95파일 (2026-07-19~) | 유료 · 일 $3 상한 |
| yt | 8일 | ~27일 |
| sonic | **4일** | ~23일 |

프리미엄 SaaS의 화면은 **추세**를 말해야 하는데 sonic은 4일이다. **수집을 서버로 옮기는 것이 곧 제품 깊이를 버는 일**이라 §2.2가 1순위인 이유가 여기서도 나온다.

---

## 3. 결정해야 할 것 (도메인 소유자 · 순서대로)

> **2026-07-31: 넷이 답을 받았다(D-056).** 결정 3·4는 여전히 열려 있다.

| # | 결정 | 답 | 왜 이것이 먼저였나 |
|---|---|---|---|
| 1 | **누가 쓰는가** | ✅ **공개 데모 · 포트폴리오 우선** | 🔴 이 답이 §2.1을 되돌렸다. 인증·DB·테넌시·과금이 전부 불필요해져 **`AGENTS.md` §3을 고치지 않는다.** 읽는 사람이 여럿인 것과 격리할 계정이 여럿인 것은 다른 문제였다 |
| 2 | **무엇을 파는가** | ✅ **리포트 열람 + 온디맨드 분석** | 열람이 먼저이고 온디맨드가 그 뒤다. 데이터 접근은 선택되지 않아 저작권 부담이 가장 큰 갈래가 빠졌다 |
| 3 | **수집을 어디로 옮기는가** | ⏳ **열려 있다** | §2.2. SaaS 때문이 아니라 **백업 없는 단일 디스크** 때문이므로 결정 1과 무관하게 남는다. 멜론 MCP 레그는 어느 쪽이든 별도 해법 |
| 4 | **저작권 검토 착수** | ⏳ **열려 있다** | 유료화 전제였는데 결정 1로 **급함이 줄었다**(공개 데모는 지금과 같은 회색). 온디맨드·유료화가 서면 다시 1순위 |
| 5 | **스크롤리텔링의 경계** | ✅ **결론 유도까지 허용** | §2.3이 "금지"로 적어 둔 칸. 불변식은 유지되고 **결론이 꼬리표를 달고 나온다**(D-039 선례) |
| 6 | **브랜드 상향 범위** | ✅ **지금 `DESIGN.md` 위에 얹는다** | 팔레트 재검증 불필요. 새 표면에도 §7.3~§7.7이 처음부터 걸린다 |

---

## 4. 제안하는 순서 (엔지니어 의견 · 승인 대상)

> 원칙: **핵심 흐름을 깨지 않고 한 층씩 올린다.** 지금 프런트를 먼저 갈아엎으면 데이터가 얕은 채로 화려한 껍데기가 되고, 수집이 PC에 묶인 문제는 그대로 남는다.

1. **수집 이전**(§2.2) — GitHub Actions cron으로 옮기고 `data/live/`를 git 안으로. **PC 의존이 사라지고 이력이 백업된다.** 프런트 작업과 병행 가능하며, 그동안 데이터 깊이가 계속 는다.
2. **읽기 전용 SaaS 껍데기** — 인증 + 조직 단위 접근. 분석은 여전히 배치. **서버가 정당해지는 조건 ①만 먼저 충족**시키고 ②는 미룬다.
3. **프리미엄 프런트 v1** — 스크롤리텔링을 **한 탭에서만** 파일럿한다(D-041이 시각화를 한 탭에서 파일럿했던 것과 같은 순서). 6탭 확산은 그다음이다.
4. **온디맨드 분석** — 여기서 처음으로 Modal 워커가 정당해진다(D-003).
5. **과금·다중 테넌시** — 1~4가 서기 전에는 붙이지 않는다.

**🔴 각 단계에서 지켜야 하는 것**: `모듈 CLI → 스키마 유효 report.json` 계약 · R1~R8 · PII 게이트 · 기준 원장 노출 · 책임소재 불변식. **이것들이 프리미엄의 내용이지 장애물이 아니다** — 반박 가능한 도구는 흔하지 않다.

---

## 5. 다음 세션이 처음 할 일

1. 이 문서 §3의 결정 1·2를 도메인 소유자에게 **먼저 묻는다.** 답 없이 아키텍처를 고르면 되돌린다.
2. 답을 기다리는 동안 막히지 않는 것: **수집 이전 조사**(§2.2 · GitHub Actions에서 유료 키·모델 캐시·MCP 레그가 각각 어떻게 되는지 실측) · **스크롤리텔링 파일럿 대상 탭 고르기**(§2.3).
3. 🔴 **`docs/DECISIONS.md`에 새 결정으로 남기기 전에는 `AGENTS.md` §3을 고치지 않는다.** 규칙을 먼저 지우고 나중에 근거를 적는 순서가 되면, 왜 그렇게 정했는지가 사라진다.

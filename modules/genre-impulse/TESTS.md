# genre-impulse · TESTS (수용 조건)

> smoke는 네트워크 0, 커밋 픽스처만. `PYTHONPATH=modules/genre-impulse/src python -m genre_impulse selftest`.

## §1. 핵심 흐름

1. **스키마 유효**: 픽스처(합성 sonic 스냅샷 + 원장 픽스처 2건 — 규칙 있는 것 1·없는 것 1) → `report.json`이 report-schema 검증 통과.
2. **결정성**: 같은 픽스처 2회 실행 → `generatedAt` 제외 동일 산출.
3. **빈 입력 graceful**: 코호트 0곡 / 원장 0건 / 규칙 0건 각각에서 죽지 않고 insights에 명시.

## §2. 원장 로드

4. 스키마 위반 임펄스 레코드 → 스킵 + insights 보고(조용한 무시 금지).
5. 확실성 등급 병기: 리포트에 인용된 원장 사실에 등급 문자열이 붙는다. **중간 미만 등급은 표면에 없다.**

## §3. 검출 규칙

6. **양성**: organic_ratio 최하위 + spectral_flatness 최상위인 합성 트랙을 코호트에 심으면 매치된다(hyperpop-texture).
7. **음성**: 전 축 중앙인 트랙은 매치되지 않는다.
8. **임계 재계산**: `--low-pct 1 --high-pct 99`로 좁히면 매치 0이 된다(튜너 배선 검증).
9. **백분위 정의 고정**: n=1 코호트에서 백분위 계산이 죽지 않는다. 동값(tie)은 `이하 비율` 정의로 결정적.
10. **falsy 함정**: `organic_ratio == 0.0`인 트랙이 "값 없음"으로 오분류되지 않는다(D-032가 기록한 `0.0 or 기본값` 함정 회귀).
18. **상한 전건(high_all) 양성**: 두 상한 축이 모두 최상위인 합성 트랙은 `ukg-origin-shuffle`에 매치된다.
19. **상한 전건 음성**: 상한 축 **하나만** 최상위인 트랙은 high_all 규칙에 매치되지 않는다(any로 구현하면 여기서 잡힌다).
20. **매치 정렬 결정성**: 규칙이 4건일 때 매치 정렬이 첫 규칙의 축에 의존하지 않는다(다른 규칙의 매치가 그 축을 안 가져도 죽지 않는다).
21. **부분 결측 튜너**: 어떤 규칙 하나의 축이 전부 있는 트랙은 다른 규칙의 축이 결측이어도 튜너 곡 목록에 남는다(결측 축은 매치 불가로만 처리).

## §4. 정직성

11. 단정 어휘 가드: report.json 직렬화 문자열에 "차트인할" · "뜰 것" · "데뷔감" 이 없다.
12. 커버리지 KPI: metrics에 `규칙 확정 n/원장 전체 N`이 존재한다.
13. 유사≠도달: insights 최상단에 비교 모집단 근사·유사≠도달 병기가 존재한다.

## §5. 시각화 계약 (v1.1 · D-041·D-042·D-043)

14. `scripts/validate_report_data.py`가 이 모듈을 **채택 모듈**로 검사(R1~R8)해 통과한다.
15. 커버리지가 **요약 도형**(`impulse-coverage`)으로 나가고, 규칙 미확정 임펄스는 **`notAnswered`**에 있다(insights 꼬리가 아니다).
16. 매치 격자(`match-axes`)의 열 이름이 **저장 키가 아니라 화면 표기**이고(`유기음 비율` 등), `scale: "value"`가 실려 **큰 값이 진하게** 칠해진다.
17. 곡별 백분위가 insights에 중복되지 않는다(규칙 귀속 줄만 존재).

### 검증 로그 (2026-07-31, v1.2 — A2 본편 규칙 2종 등재)

- **6~10·18~21** ✅ selftest 17/17 — high_all 조합(§2.1.2) 양성/음성 · 다규칙 정렬(첫 규칙 축 의존이던 정렬을 규칙별 극단 축으로) · 부분 결측 트랙의 튜너 잔류(전 축 요구를 "규칙 하나라도 평가 가능"으로 완화).
- **게이트** ✅ ruff · pyright 0 · 검사기 CLEAN · 대시보드 tsc·lint 0 · smoke:tabs 12/12 · 라이트/다크 육안(구획 2).
- **육안에서만 잡힌 결함 3건**(정적 게이트·스모크 전부 통과 상태였다): ① 격자에 규칙 귀속이 없어 여러 규칙에 걸린 곡이 중복 행 결함처럼 읽힘 → 행 접두 "짧은 규칙명 · 곡" ② 전체 규칙 id를 접두로 쓰자 곡명이 잘림 → short 표시명 ③ **매치(곡×규칙 쌍) 수를 "곡"으로 표기** — 27건을 27곡으로(실제 19곡). 타일·인사이트·튜너 요약 전부 유일 곡 수로 수정.
- **베이스라인 실측이 등재를 뒤집은 것 1건**: amapiano 2축 조합은 정답지 7/7인데 코호트 20~21%가 걸린다(축 양의 상관) → 등재 철회, RULES §2.2 기록.

### 검증 로그 (2026-07-30, v1.1)

- **14** ✅ `ADOPTED_MODULES`에 `genre-impulse` 추가 → 검사기 CLEAN(전수 6리포트). 구획 2(매치 1차트 · 기준 1차트) · 지표 4개 전부 배정 · 질문 4개 앵커 정상.
- **15** ✅ 요약 = 규칙 확정 1 / 서명 작업 중 1 / 아직 서명 없음 8. `notAnswered` 11건(고정 2 + 규칙 미확정 9). insights 32줄 → **13줄**.
- **16** ✅ 열 = `유기음 비율` · `인터샘플 피크 비율` · `스펙트럼 평탄도`. `scale:"value"` 적용 전에는 P1.0이 가장 진하고 범례가 그것을 "상위"로 불렀다(정반대로 읽히는 그림) — 프리미티브(`Heatmap`)에 방향을 payload로 올려 해소, chart-history의 순위 격자 4종은 기본값(rank)이라 무회귀.
- **17** ✅ `[매치]` 줄이 곡별 11줄 → 규칙별 1줄. 같이 발견된 결함: CLI stdout이 그 줄 수를 세고 있어 매치 수가 11 → 1로 잘못 찍혔다(지표에서 읽도록 수정).
- **게이트** ✅ ruff · pyright 0 · schema valid · 검사기 CLEAN · 대시보드 `typecheck`·`lint` 0 · 라이트/다크 육안(구획 2).

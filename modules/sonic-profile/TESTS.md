# sonic-profile · TESTS (값 무결성 검증)

> [`RULES.md`](RULES.md) §3 원장의 각 항목은 여기서 검증된다. **네트워크 없이 도는 것**이 스모크의 조건.
> 결정 근거: [`../../docs/DECISIONS.md`](../../docs/DECISIONS.md) D-019.

## 1. 픽스처 전략 — 합성 신호 (저작물 미사용)

원본 오디오를 저장하지 않는 것이 이 모듈의 불변식이므로(RULES §1), **픽스처도 음원이 아니다.** numpy로 생성한 **합성 신호**를 쓴다 — 정답을 우리가 알고 있다는 점에서 오히려 검증에 유리하다.

| 픽스처 | 생성 | 검증하는 것 |
|---|---|---|
| `click_120bpm` | 120 BPM 간격 클릭 트레인 | tempo_bpm이 120(±허용오차), pulse_clarity 높음 |
| `click_90bpm` | 90 BPM 클릭 | **옥타브 오류 회귀 방지**(180·45로 튀지 않는가) |
| `noise_white` | 백색잡음 | pulse_clarity 낮음, tempo 신뢰도 플래그 off |
| `sine_50hz` + `sine_4khz` | 저역/고역 정현파 | low_end_ratio가 각각 ~1.0 / ~0.0, brightness 단조 증가 |
| `silence` | 무음 | **미해석 처리**(0으로 채우지 않음) |
| `too_short` | 1초 | 길이 미달 → 미해석 |

## 2. 필수 통과 항목

- **결정성**: 같은 입력 → 같은 출력. 동일 픽스처를 2회 분석해 **비트 단위로 동일**해야 한다(난수·시각 의존 금지).
- **미해석 규약**: 무음·과단축·디코드 실패가 `0`이 아니라 **결측**으로 나오는가. 결측이 집계 평균을 끌어내리지 않는가(결측 ≠ 0, §0).
- **스키마**: `sonic-profile validate output/report.json` 통과.
- **관측 0 케이스**: 입력이 비어도 크래시하지 않고 유효 report + "관측 없음" insight를 내는가.
- **한계 문구 전파**: report의 `insights`에 RULES §3.1의 "곡 간 단일 비교 금지"와 커버리지가 **실제로 들어 있는가**(문자열 존재 검사). 한계 병기는 선택이 아니라 §5 준수 요건이므로 테스트한다.
- **무보관**: `fetch` 실행 후 임시 디렉터리에 오디오 파일이 **남아 있지 않은가**. 예외 발생 경로에서도 삭제되는가(실패 주입 테스트).

## 3. `crest_factor_db` 검증 — **통과 (2026-07-28)** → 활성화

**질문**: Apple 프리뷰에 라우드니스 정규화가 걸려 있는가? 걸려 있다면 crest factor는 마스터링이 아니라 Apple의 처리 파이프라인을 측정하게 된다.

**설계**: 압축 강도가 뚜렷이 다를 것으로 예상되는 10곡(현대 EDM/K-pop/힙합 vs 클래식·재즈·1960~70년대 녹음·앰비언트)의 프리뷰 RMS·peak를 비교. 정규화가 걸려 있으면 RMS가 좁게 뭉친다. 판정 기준은 **결과를 보기 전에** 고정: 스프레드 < 2dB → 정규화 의심(비활성 유지) · ≥ 6dB → 정규화 없음(활성 가능).

**결과**: RMS 범위 **−24.50 ~ −5.05 dB (스프레드 19.46 dB, σ 6.34)** — 기준을 크게 넘겼다. 순서도 마스터링 이론과 일치한다.

| 표본 | RMS | crest |
|---|---|---|
| 현대 K-pop (CORTIS - REDRED) | −5.05 dB | 8.23 dB |
| 현대 힙합 (Kendrick Lamar) | −5.81 dB | 9.35 dB |
| 현대 EDM (David Guetta) | −7.73 dB | 11.02 dB |
| 1960s 팝 (The Beatles) | −11.64 dB | 13.97 dB |
| 1970s 록 (Pink Floyd) | −14.47 dB | 14.82 dB |
| 재즈 (Bill Evans Trio) | −17.14 dB | 16.12 dB |
| 클래식 피아노 (Debussy) | −20.10 dB | 17.52 dB |
| 클래식 관현악 (Mahler) | −24.50 dB | 14.79 dB |

**판정**: 프리뷰는 **정규화되지 않으며** 마스터링 압축을 실제로 반영한다 → 지표 활성, RULES §3 표와 D-019에 반영. 상태 문자열이 `validated-2026-07-28`로 바뀌었고 selftest가 회귀 가드로 이를 검사한다.

**이 검증의 한계**: US 스토어프론트 · n=10 · 단일 시점. Apple이 정책을 바꾸면 무효가 되므로 **재검증 가능하도록 스크립트 설계를 위 문단에 남긴다.**

## 3.1 검증 로그 (2026-07-28, v1 관통)

- **selftest 10/10 PASS** (합성 신호·네트워크 0): tempo 120→117.45 · 90→89.1(옥타브 오류 회귀 없음) · 펄스 명료도 클릭 0.8826 > 잡음 0.0894 · 50Hz 저역비 1.0 / 4kHz 0.0 · 밝기 53.1 < 4051.3 · 무음·1초 입력 미해석 · 결정성 · crest 미검증 표기.
- **라이브 관통**: 워치리스트 11팀 → **8팀 해석**(izna 107.67BPM · KATSEYE 152.0 · Hearts2Hearts 129.2 · RESCENE 112.35 · CORTIS 152.0 · KEYVEATZ 129.2 · BABYMONSTER/MEOVV 117.45), 3팀 미해석(KiiiKiii·ILLIT·KISS OF LIFE — Apple m4a만 존재, PyAV 미설치).
- **별칭 검증 작동**: `KISS OF LIFE` → Sade 오매칭이 차단되어 미해석 처리됨(RULES §1).
- **무보관 검증**: `fetch` 후 임시 디렉터리에 `sonic_*` 잔존 **0건**.
- **게이트**: ruff · pyright 0 errors · `analyze` schema valid · 대시보드 5번째 탭 렌더(한계 문구 3종 출력 확인).

## 4. 태거 전처리 회귀 검증 (RULES §3.3) — **이 수치가 떨어지면 전처리가 깨진 것이다**

장르·악기 모델은 Essentia와 **동일한 mel**을 먹어야 한다. 어긋나면 예외가 아니라 **그럴듯한 오답**이 나오므로 일반 테스트로는 안 잡힌다. 그래서 **정답을 아는 신호**를 회귀 가드로 쓴다.

**가드**: Apple KR 차트 코호트에서 `Pop---K-pop`이 400개 스타일 중 **상위 5위 안에 드는 곡의 비율**.

| 전처리 | K-pop 중앙순위 | 평균 확률 | 상위5 진입 |
|---|---|---|---|
| mel `type=magnitude` (틀림) | 150위 | 0.0008 | 0/12 |
| **mel `type=power` (정답)** | **1위** | **0.4943** | **11/16** |

- **기준선 11/16**(2026-07-29, 코호트 앞 16곡). 이보다 크게 떨어지면 전처리 회귀를 의심한다.
- 판정 기준은 결과를 보기 전에 정했다: *"K-pop 차트 곡인데 K-pop 라벨이 상위에 없으면 전처리가 틀린 것"*.
- 격자 탐색으로 확인한 민감 파라미터: `power`(결정적) · 필터뱅크 `norm=slaney`(결정적) · `center`(경미) · 스펙트럼 스케일(경미).

**한계**: 이건 *전처리 동일성* 가드이지 *분류 정확도* 측정이 아니다. 정확도는 사람 라벨 50~100곡으로 별도 측정해야 하며(RULES §3.1.7), 그전까지 리포트는 `정확도 미측정`을 병기한다.

### 4.1 상위 k 절단 회귀 (RULES §3.1.6.1)

**임계로 곡 수를 세는 축은 라벨을 잘라 저장하면 하한이 된다.** 이건 예외가 아니라 그럴듯한 숫자로 나와서 일반 테스트에 안 걸린다 — **저장된 라벨 수를 직접 센다.**

| 검증 | 방법 | 기대 |
|---|---|---|
| 악기 라벨 완전성 | 스냅샷의 `features.instruments` 길이 | 전 레코드 **40**(= `TOP_K_INSTRUMENT`). 미만이면 `retag` 대상 |
| `retag` 멱등 | 연속 2회 실행 | 2회차 "복구 대상 없음" · **네트워크 요청 0** |
| 캐시 무효화 | `tagger_top_k_instrument`가 `engine_key`에 포함 | k를 바꾸면 캐시 미적중 → 콜드 실행. 빠지면 **다음 수집이 절단본을 되살린다** |
| 하한 표시 | `floor > 임계`인 절단 트랙 | insight·화면에 "곡 수가 하한인 곡 N"이 뜬다(복구 후에는 0이라 문구가 사라진다) |

**실행 로그(2026-07-29)**: 절단 108레코드 / 고유 101녹음 → **전건 복구, 실패 0**. 복구 후 라벨 수 min=max=40, 하한 경고 소멸. 임계별 악기 종수가 0.3→10종 · 0.2→12종 · 0.1→16종으로, **절단 때는 보이지 않던 저확률 악기가 드러났다**(예: `drums` 임계 0.1에서 57 → 100곡).

## 5. 리듬 패턴 검증 (RULES §3.1.5)

selftest가 **모델 없이**(네트워크 0) 순수 함수를 검증한다.

| 검증 | 방법 | 기대 |
|---|---|---|
| 템포 적합이 양자화를 씻는가 | 0.02초 격자에 붙인 128·143BPM 비트 시각 | 적합 오차 < 0.2%, `median(diff)`보다 정확 (실측: 128.00 vs 130.43) |
| 마디 프로파일 정규화 | 합성 정박 킥 | 합 = 1 |
| 패턴 정합 | 합성 정박 킥 | `four-on-floor`가 최고 정합 |
| 결정성 | 같은 프로파일 2회 | 동일 |
| 미해석 규약 | 비트 < 4 · 다운비트 < 3 | `RhythmUnavailable`(0으로 채우지 않음) |
| **템플릿 이름 ↔ 위치 정합** | `tresillo(8분 3+3+2)` 위치 | `(0,6,12)` — 8분음 n = 칸 2n. **이름과 격자가 어긋나면 실패**(2026-07-29 결함) |
| **16분 tresillo = dembow 중복** | 반마디 `(0,3,6)`을 마디 끝까지 타일링 | `(0,3,6,8,11,14)` = `dembow` 템플릿과 **동일 집합**임을 명시적으로 확인(원장 결함 ①이 사라지면 이 테스트도 갱신) |
| **비직교성 상한** | 전 템플릿 쌍 코사인 | 최대 쌍 상관을 계산해 **0.85 초과 시 실패**. 초과는 "두 이름이 같은 것을 재고 있다"는 뜻이라 원장 재검토 신호 (현재 최악 0.83) |
| **θ 미만 → 해당 없음** | 어느 템플릿도 쓰지 않는 칸(1·5·9·13)에만 킥 | 전 템플릿과 음의 상관(실측 −0.2182) → `assigned=None`. **argmax가 이름을 뱉지 않는가** |
| **평탄 프로파일 폴백 가드** | 완전 균일한 16칸 프로파일 | 전 정합도 0 → `assigned=None`. θ를 0으로 내리면 배정이 되살아나는 것까지 확인(임계가 유일한 방어선) |
| **동점 검출** | `tie_gap`을 열어 둔 정박 프로파일 | `tie=True`로 표시되고 배정은 유지된다 — **집계에서 지워지지 않는다** |

> **픽스처 함정(실측)**: 임펄스 프레임을 `int()`로 잘라 넣으면 0.5초가 0.4993초가 되어 칸 경계에서 **앞 칸으로 밀린다**(4번 → 3번). 그 상태에서는 정박 킥이 `dembow`로 잡힌다. 격자 정렬 픽스처는 반드시 **반올림**한다.

> **폴백 함정(실측 2026-07-29)**: `match_templates`는 프로파일이 완전 평탄하면 노름 0으로 전 정합도를 0으로 돌려주는데, 여기에 `max()`를 씌우면 **사전 삽입 순서상 첫 키인 `four-on-floor`가 조용히 당첨된다**. "리듬 없음"이 "정박"으로 둔갑하는 경로다. `classify_rhythm`은 (a) 동점을 **이름순**으로 깨 삽입 순서 의존을 없애고 (b) θ로 배정 자체를 막는다. θ를 0으로 내리면 되살아나므로 두 경로 모두 테스트로 고정한다.

> **음의 상관 픽스처 함정(실측)**: "정박의 반대 위상"(2·6·10·14)은 이 테스트에 **부적합하다** — `four-on-floor`와는 음의 상관이지만 `trap-synco`와 +0.33이라 argmax가 다른 이름으로 도피한다. 임계가 막아야 하는 것이 바로 그 도피이므로, 픽스처는 **어느 템플릿도 쓰지 않는 칸**(1·5·9·13)을 써서 전 템플릿과 음의 상관을 만든다.

**실측 분포 (2026-07-29 · Apple KR 코호트 108곡)** — θ와 동점폭 기본값의 근거이자 한계:

| 항목 | 값 |
|---|---|
| 최고 정합도 분포 | min **−0.027** · p10 0.480 · 중앙 0.685 · p90 0.872 · max 0.969 |
| θ=0.30 미만 (해당 없음) | **4곡** (그중 1곡은 **음의 상관** — θ 없이는 `backbeat`로 집계됐다) |
| 1위−2위 차 중앙값 | 0.110 |
| 동점(차 < 0.05) | **29곡 / 108 = 27%** |
| 최소 격차 | 0.0038 (에스파 - Supernatural) |

- **이 수치들이 "판정 아닌 순위"의 실증이다.** 코호트의 1/4이 사실상 동점인데 막대그래프는 그 사실을 지운다 → RULES §4 리듬 뷰 규약이 동점 곡 수 병기를 강제하는 근거.
- **θ=0.30·동점폭 0.05는 관습값이다.** 위 분포를 보고 정한 것이므로 **케이스 오버핏 위험이 있다**(AGENTS §2.1: 기준은 도메인 근거에서 정한 뒤 결과를 본다). A&R가 재조정할 때까지 잠정값으로 표기한다.

**아직 검증하지 못한 것**: 실음악에서의 **다운비트 정확도**. 합성 클릭에는 화성·베이스 단서가 없어 픽스처가 다운비트를 과소평가한다(합성 F 0.34~0.68). 정답 채보가 붙은 코퍼스(AI Hub 등)가 생기기 전까지 `beats_per_bar`는 **집계에만** 쓴다.

## 6. 신규 지표 검증 (D-031)

전부 **정답을 아는 합성 신호**로 고정한다(§1 전략). 네트워크 0.

### 6.1 리듬 파생 3종 — 저장된 프로파일에서 재계산되므로 오디오가 필요 없다

| 검증 | 픽스처 | 기대 |
|---|---|---|
| `syncopation_ratio` 하한 | 정박만 있는 프로파일(0·4·8·12) | **정확히 0.0** |
| `syncopation_ratio` 상한 | 정박 칸이 완전히 빈 프로파일 | **정확히 1.0** |
| `bar_profile_contrast` 기준점 | 완전 균일 16칸 | **정확히 1.0** (균일이 1이 아니면 정의가 깨진 것) |
| `bar_profile_contrast` 단조성 | 한 칸에 집중 | 16.0 (= bins) 에 접근 |
| `grid_deviation_ms` 하한 | 완벽한 등간격 비트 시각 | **≈0** (부동소수 오차 한계) |
| `grid_deviation_ms` 단조성 | 지터 σ를 키운 비트 시각 | 잔차가 **단조 증가** |
| **소급 적용 일치** | 저장된 `kick_bar_profile`에서 재계산한 값 | 오디오에서 직접 잰 값과 **동일**(재계산 경로가 정본과 갈라지지 않는가) |

### 6.2 신규 DSP 3종

| 검증 | 픽스처 | 기대 |
|---|---|---|
| `loudness_lufs` **선형성** | 같은 신호를 −6dB로 스케일 | 정확히 **−6 LUFS** 이동 (K-weighting은 선형이므로 이건 구현 정오를 가른다) |
| `loudness_lufs` 상대순서 | 정현파 vs 백색잡음 vs 무음 | 무음은 **미해석**(−inf를 0으로 채우지 않는다, §0) |
| `spectral_flatness` 하한 | 단일 정현파 | **≈0** (순음 = 완전 톤성) |
| `spectral_flatness` 상한 | 백색잡음 | **≈1** 에 접근 |
| `stereo_width` 하한 | L = R (동일 채널) | **정확히 0.0** |
| `stereo_width` 상한 | L = −R (역위상) | **정확히 1.0** |
| `stereo_width` 모노 소스 | 단일 채널 입력 | 0.0 이되 **"좁다"가 아니라 "정보 없음"**으로 표기되는가 |
| **🔴 모노 무회귀** | 스테레오 로드 → width 계산 → 다운믹스 | 기존 모노 경로와 **모든 §3 지표가 동일**해야 한다 |

> **모노 무회귀가 이 확장의 최대 위험이다.** `stereo_width` 하나 때문에 로드 경로를 바꾸는데, 그 과정에서 `tempo`·`low_end_ratio` 등 **기존 전 지표가 조용히 바뀌면 과거 값과 비교 불가**가 된다(RULES §2). 리샘플·다운믹스 **순서**가 바뀌면 부동소수 말단이 흔들릴 수 있으므로, 이건 추론이 아니라 **양 경로를 같은 입력에 돌려 비교**로 확정한다. 어긋나면 저장 정밀도(반올림 자리) 안에 드는지까지 확인한다.

### 6.3 구성물 지표 (C층 · RULES §3.1.6.2)

| 검증 | 방법 | 기대 |
|---|---|---|
| 임베딩 재사용 | `danceability`·`moods`가 기존 effnet 1280을 쓰는가 | **effnet 추론 횟수가 늘지 않는다**(편승 확인 — 늘었다면 배선이 틀린 것) |
| msd-musicnn 패치 | 입력 텐서 shape | **(187, 96)** — effnet의 128과 다르다. 128을 그대로 먹이면 조용히 틀린 값이 나온다 |
| mel 공유 | musicnn mel이 두 체인에 같은 사양으로 쓰이는가 | `TAG_SR·N_FFT·HOP·N_MELS` 동일, **패치 길이만** 다름 |
| 결정성 | 같은 입력 2회 | 동일(§2) |
| 척도 보존 | valence·arousal 원값 | **임의 정규화하지 않는다** — 데이터셋 척도 그대로 저장 |
| 실측 범위 기록 | 코호트 전건의 출력 분포 | 원장(§3.1.6.2)에 **관측된 min·중앙·max를 기입**한다 |
| 한계 문구 전파 | report `insights` 문자열 검사 | `정확도 미측정` + **학습 모집단이 K-pop이 아님**이 실제로 들어 있는가 |
| **전처리 회귀(기존)** | §4의 K-pop 상위5 진입률 | **11/16 유지** — 태거 배선을 건드렸으므로 반드시 재측정. ✅ 2026-07-29 실측 **11/16**(중앙 순위 1.5) |

### 6.4 🔴 붕괴 가드 — 구성물 지표는 **대조 표본에서 순서가 맞아야** 채택된다

**이 검사가 없었으면 퇴화한 모델을 실었을 것이다.** 실제로 그럴 뻔했다(D-031).

C층 지표는 값이 그럴듯한 범위로 나오기 때문에 **일반 테스트에 걸리지 않는다.** `muse` 헤드는 예외도 NaN도 내지 않았고, 5.2~5.5라는 완벽히 정상적인 숫자를 냈다 — 단지 **모든 음악에 같은 값**이었을 뿐이다. §4의 전처리 가드와 같은 논리이며, 대상이 전처리가 아니라 **헤드**다.

**방법**: 정서가·리듬성이 극단적으로 갈릴 것이 자명한 대조 표본을 쓰고, **판정 기준을 결과 보기 전에 고정**한다.

| 표본 | 예상 |
|---|---|
| 말러 교향곡 6번 "비극적" · 사티 짐노페디 | 낮은 각성도 · 낮은 danceability |
| 빌 에반스 재즈 | 중간 |
| 슬립낫 · Happy · 스크릴렉스 | 높은 각성도 · 높은 danceability |

**판정**: 대조 표본에서 **폭이 척도의 10% 미만이면 붕괴로 보고 채택하지 않는다.**

| 헤드 | valence 폭 | 순서 | 판정 |
|---|---|---|---|
| `muse-msd-musicnn-1` | **0.290** | 없음 (말러 5.249 · Happy 5.469 · 스크릴렉스 5.299) | ❌ **배제** |
| `deam-msd-musicnn-1` | **3.197** | 정확 (사티 3.29 < 재즈 4.05 < 말러 4.39 < Happy 5.42 < 스크릴렉스 6.19) | ✅ **채택** |
| `emomusic-msd-musicnn-1` | 1.666 | 정확 | ✅ 대안 |
| `danceability-discogs-effnet-1` | 0.004 → 1.000 | 정확 | ✅ 채택(단 K-pop 안에서는 천장) |

> **배선인지 모델인지 가르는 법**: 세 헤드를 **같은 임베딩·같은 코드 경로**에 통과시킨다. 하나만 붕괴하면 헤드 문제다. 추가로 이 헤드들은 **선형 단층**이라 패치별 출력 평균 = 임베딩 평균의 출력이 **정확히 일치**해야 한다 — 어긋나면 그때는 배선 문제다(실측 일치 확인).

**네트워크를 타므로 selftest에 넣지 않는다**(스모크는 네트워크 0이 조건). §3의 crest 검증과 같이 **모델·헤드를 바꿀 때 돌리는 수동 검사**다.

## 7. T0 축 검증 (D-032) — selftest 60개 중 19개

전부 합성 신호·순수 함수라 **네트워크 0**이다.

| 검증 | 픽스처 | 기대 |
|---|---|---|
| `organic_ratio` 상·하한 | 유기음만 / 전자음만 | 1.0 / 0.0 |
| 모호 라벨 배제 | piano 0.5 + synth 0.5 + **bass 0.9** | 0.5 (bass가 분모에서 빠짐) |
| 모호 라벨만 있을 때 | bass + guitar | **미해석**(0으로 채우지 않음) |
| `derive_all` 비파괴 | 이미 값이 있는 dict | 덮어쓰지 않는다 |
| `bar_profile_entropy` | 완전 균일 | 정확히 1.0 |
| `bar_half_asymmetry` | 앞 반마디에만 킥 | 1.0 |
| **LRA 하한** | 일정 진폭 정현파 | ≈0 LU |
| **LRA 단조성** | 조용한 10초 + 큰 10초 | 26.0 LU ≫ 0 |
| `over_unity_ratio` | 진폭 1.5 정현파 / 정상 신호 | >0.3 / 정확히 0 |
| `silence_ratio` | 절반이 무음 | ≈0.5 |
| 밴드별 스테레오 폭 | L = R | 전 대역 0 |
| 위상 상관 | L=R / L=−R | +1.0 / −1.0 |
| **`ioi_entropy` 하한** | 완전 등간격 온셋 | **정확히 0**(미해석 아님) |
| `swing_ratio` 기준점 | 비트 정중앙 온셋 | 0.5 |
| `swing_ratio` 단조성 | 셔플 위치 온셋 | 0.667 > 0.5 |
| 마디 자기유사도 | 같은 패턴 6마디 반복 | >0.9 |

> **픽스처 함정 두 번 걸렸다 — 둘 다 이 저장소가 이미 아는 것이었다.**
>
> 1. **격자 경계 정밀도**(§5의 함정과 동형): 마디 첫 칸에 임펄스를 놓으면 부동소수 오차로 **앞 마디에 밀려** 자기유사도가 1.0이 아니라 0.667이 나온다. 임펄스는 칸 **안쪽**에 둔다.
> 2. **`0.0 or 기본값`**: `(ioi_entropy(x) or 1.0) < 0.05` 는 값이 정확히 0일 때 falsy라 **기본값 1.0으로 바뀌어 하한 검사가 항상 실패**한다. 하한을 검사할 때 `or`로 기본값을 주면 안 된다.
>
> 그리고 이 두 번째 함정이 **진짜 결함 하나를 드러냈다**: `ioi_entropy`가 완전히 규칙적인(기계적인) 리듬에 `None`을 돌려주고 있었다. 히스토그램이 한 칸에 몰리면 엔트로피는 **0이지 미해석이 아니다** — 가장 정확한 입력이 결측이 되던 경로였다.

## 7.1 판별력 실측 (2026-07-29 · 코호트 108곡) — **이게 표면 선별의 근거다**

축을 늘리는 것과 축이 쓸모 있는 것은 다른 문제다. 71개 스칼라 축 전부에 대해 `p10~p90` 폭을 중앙값으로 나눈 상대 분산을 쟀다.

| 판정 | 개수 | 대표 |
|---|---|---|
| **강함**(≥0.5) | 47 | `stereo_width_low` 3.23 · `loudness_range_lu` 1.92 · `organic_ratio` 1.30 · `rhythm_self_similarity` 1.04 |
| 보통(0.15~0.5) | 18 | `phase_correlation` 0.41 · `valence` 0.34 · `syncopation_ratio` 0.19 |
| **약함**(<0.15) | 6 | `bar_half_asymmetry` 0.129 · `swing_ratio` 0.081 · `chroma_entropy` 0.059 · `bar_profile_entropy` 0.037 |
| 상수 | 3 | `duration_s` · `silence_ratio` · `tag_patches`(프리뷰가 전부 30초라 당연) |

- **지표 타일에는 24개만 올린다**(RULES §3.7.2). 나머지는 스냅샷·시리즈에 그대로 있다 — 지운 것이 아니라 표면에 안 올린 것이다.
- **`dc_offset`은 결함을 하나도 못 찾았다**(전 곡 −0.002~0.001). 이건 실패가 아니라 **"이 소스에 DC 결함이 없다"는 정보**다.
- 이 표는 **다음에 축을 늘릴 때 다시 돌린다** — 코호트가 바뀌면 판별력도 바뀐다.

## 7.2 스템 분리 축 수용조건 (RULES §3.8 · **구현 전 선언**)

> 이 절은 **아직 실행되지 않았다.** RULES §3.8이 정의 먼저를 지킨 것처럼, 여기서는 **수용조건을 먼저 못박는다** — 구현 후에 조건을 정하면 결과에 맞춰 조건이 움직인다(AGENTS §2.1 케이스 오버핏 경계).

### 7.2.1 합성 픽스처 (네트워크 0 · 정답을 아는 신호) — **구현·통과 2026-07-30, 17항**

실행: `PYTHONPATH=modules/sonic-profile/src python -c "from sonic_profile.stems import selftest_stems; selftest_stems()"`(모델도 필요 없다).

| 검증 | 픽스처 | 기대 | 실측 |
|---|---|---|---|
| `halftime_snare_ratio` 하한 | 4·12칸에만 스네어(정통 백비트) | **정확히 0** (하프타임 아님이 0으로 나와야 결측과 구별된다) | 0.0 ✓ |
| `halftime_snare_ratio` 분모 0 | 8칸에만 스네어 | **미해석**(∞를 큰 수로 채우지 않는다, §0) | None ✓ |
| `halftime_snare_ratio` 기준점 | 4·8·12 균등 | **정확히 0.5** | 0.5 ✓ |
| `snare_backbeat_ratio` 대칭 | 위 두 픽스처 | 백비트 1.0 ↔ 하프타임 0.0 | ✓ |
| 유효성 게이트(차단) | 대비 1.0 | 하위 스네어 축 **전부 결측** + `snare_axes_gated` | ✓ |
| 유효성 게이트(통과) | 대비 2.0 | 값 유지 | ✓ |
| `bass_glide_ratio` 하한 | 고정 f0 | **정확히 0** | 0.0 ✓ |
| `bass_glide_ratio` 상한 | 선형 스윕 **≈21 st/s** | **>0.95** | 0.99 ✓ |
| `bass_glide_ratio` 계단 배제 | 글라이드 없이 **음만 바뀌는** 계단형 f0 | **정확히 0** — 이 항이 정의의 핵심이다 | 0.0 ✓ |
| 옥타브 오류 내성 | 한 프레임만 f0가 2배로 튐 | 글라이드로 **세지 않는다** | 0.0 ✓ |
| 격자 밀착 상한 | f0가 반음 격자에 정확히 붙음 | **≈1.0** | 1.000 ✓ |
| 격자 밀착 하한 | 격자에서 50센트(최대 이탈) | **≈0** | 0.000 ✓ |
| `bass_note_stability` 대칭 | 스윕 vs 고정음 | 스윕 < 고정음 | 0.52 < 1.0 ✓ |
| `vocal_vibrato_strength` 대역 선택성 | 6Hz 변조 vs **같은 깊이 1Hz** | 전자 > 0.8 **이고** 후자의 5배 초과 | 1.00 vs 8.6e-12 ✓ |
| 무변조 f0 | 완전 직선 | **미해석**(0으로 채우지 않는다) | None ✓ |
| 전건 무성 | f0 전부 NaN | **미해석** | ✓ |
| 프록시 분모 0 | f0 중앙값 0 / 중심 None | **미해석** | ✓ |

> **작성 중 픽스처 두 개가 자기 선언을 안 지키고 있었다.** 기록해 둔다 — 다음에 축을 늘릴 때 같은 실수를 한다.
>
> 1. "기울기 ≫ G"라 적어 놓고 실제로는 24세미톤/200프레임 = **5.2 st/s로 임계 6.0 아래**였다. 코드가 아니라 픽스처가 틀렸고, 코드를 고쳤다면 임계를 무력화할 뻔했다.
> 2. **"무변조"는 미해석이 정답이다.** 완전 직선 f0는 총 전력이 0이라 비율이 정의되지 않는다. 그래서 대조군을 **같은 깊이의 1Hz 흔들림**으로 바꿨다 — 이 축의 주장은 "변조가 있다"가 아니라 **"4~8Hz 대역을 고른다"** 이므로 대역 밖 변조와 대조하는 것이 옳다. 완전 직선은 별도 항으로 남겼다.

### 7.2.1-a 무보관 불변식 (모델 필요 · opt-in) — **통과 2026-07-30**

| 검증 | 방법 | 기대 | 실측 |
|---|---|---|---|
| 4스템 반환 | 2초 합성 스테레오 분리 | `drums·bass·other·vocals` | ✓ |
| **무보관** | 분리 전후 임시 디렉터리 diff | **스템 파일 0개** | 0개 ✓ |

**이 불변식은 테스트가 아니라 구조로 보증된다.** `separate()`의 서명에 **경로 인자가 없고** demucs CLI(결과를 디스크에 쓴다)를 쓰지 않는다 — `apply_model`에 텐서를 직접 넣으므로 "삭제를 잊는" 경로가 애초에 존재하지 않는다. 위 검사는 그 성질이 유지되는지 보는 감시다.

### 7.2.2 정답지 채택 게이트 (실측 — 이걸 통과해야 원장에 오른다)

정답지는 [`../../data/research/genre-impulse/cohort_cases.json`](../../data/research/genre-impulse/cohort_cases.json)에 이미 있다. **코호트에 섞어 넣고 백분위를 본다**(genre-impulse TESTS §3와 같은 방식).

| 축 | 정답지 | 통과 기준 | 실패 시 |
|---|---|---|---|
| `halftime_snare_ratio` | 저지클럽 원형 5곡 | 코호트 상위 20% 안 **3곡 이상** | 원장에 "판별력 없음"으로 적고 검출 규칙 승격 보류 |
| `bass_glide_ratio` | 드릴 원형 3곡 | 코호트 상위 20% 안 **2곡 이상** | 동일 — A2.1의 "드릴 축 공백"이 **스템으로도 안 풀린 것**이므로 그대로 기록한다 |
| `vocal_tuning_hardness` | 하이퍼팝 계보 5곡 | 코호트 상위 20% 안 **3곡 이상** | 동일 |
| `vocal_pitch_shift_proxy` | 하이퍼팝 계보 5곡 | 위와 동일 | 🔺 프록시라 **철회 가능성이 가장 높은 축** — 실패하면 `meter_duple_bias` 선례대로 **해석을 버리고 원시값만 남긴다** |

### 7.2.3 분리 품질 회귀 (스템이 실제로 마스킹을 풀었는가)

2026-07-29 실측(분리 전 중역 대비 1.17~1.41 → 분리 후 1.56~2.38)이 기준선이다. **같은 3곡으로 재현**해 분리 후 대비가 저역 기준선 **1.71을 2곡 이상에서 넘는지** 확인한다. 못 넘으면 스템 도입의 전제가 무너진 것이므로 **비용(+20~25분/일)을 지불할 근거가 없다** — 이 회귀는 모델·버전을 바꿀 때마다 돌린다(§6.4 붕괴 가드와 같은 성격).

## 8. 재현 레시피

```bash
# 스모크(네트워크 0) — 합성 픽스처로 전 구간 관통
PYTHONPATH=modules/sonic-profile/src python -m sonic_profile analyze \
  modules/sonic-profile/tests/fixtures/synthetic -o modules/sonic-profile/output/
PYTHONPATH=modules/sonic-profile/src python -m sonic_profile validate \
  modules/sonic-profile/output/report.json

# 라이브 1회(워치리스트 대상, 오디오 무보관)
PYTHONPATH=modules/sonic-profile/src python -m sonic_profile fetch \
  --watchlist packages/entity-master/watchlist.json -o data/live/sonic/<date>.json

# 상위 k 절단으로 잘린 과거 악기 라벨 복구 (멱등 — 2회차는 no-op·네트워크 0)
PYTHONPATH=modules/sonic-profile/src python -m sonic_profile retag data/live/sonic --dry-run
PYTHONPATH=modules/sonic-profile/src python -m sonic_profile retag data/live/sonic

# 게이트
python -m ruff check modules/sonic-profile
python -m pyright modules/sonic-profile
```
